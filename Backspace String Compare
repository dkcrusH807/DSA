    def backspaceCompare(self, s, t):
        def build(string):
            st=[]
            for ch in string:
                if ch=="#":
                    if st:
                        st.pop()
                else:
                    st.append(ch)
            return "".join(st)
        return build(s)==build(t)

	def nextSmallerEle(self, arr):
		n=len(arr)
		res=[-1]*n
		st=[]
		for i in range(n-1,-1,-1):
		    while st and st[-1]>=arr[i]:
		        st.pop()
		    if st:
		        res[i]=st[-1]
		    st.append(arr[i])
		return res

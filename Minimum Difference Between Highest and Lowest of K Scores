def mindiff(nums,k):
    nums.sort()
    res=float('inf')
    n=len(nums)
    for i in range(n-k+1):
        diff=nums[i+k-1]-nums[i]
        res=min(res,diff)
    return res
print(mindiff([9,4,1,7],2))

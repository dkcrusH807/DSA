def maxavg(nums,k):
    n=len(nums)
    winsum=sum(nums[:k])
    maxsum=winsum
    for i in range(k,n):
        winsum+=nums[i]-nums[i-k]
        maxsum=max(maxsum,winsum)
    return maxsum/k
print(maxavg([1,12,-5,-6,50,3],4))

    def minOperations(self, logs):
        st=[]
        for l in logs:
            if l=="../":
                if st:
                    st.pop()
            elif l=="./":
                continue
            else:
                st.append(l)
        return len(st)

    def findWords(self, words):
        rows=[set("qwertyuiop"),set("asdfghjkl"),set("zxcvbnm")]
        ans=[]
        for w in words:
            l=set(w.lower())
            if any(l.issubset(r) for r in rows):
                ans.append(w)
        return ans

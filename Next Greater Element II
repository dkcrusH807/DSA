    def nextGreaterElements(self, nums):
        n=len(nums)
        res=[-1]*n
        st=[]
        for i in range(2*n):
            num=nums[i%n]
            while st and nums[st[-1]]<num:
                res[st.pop()]=num
            if i<n:
                st.append(i)
        return res
